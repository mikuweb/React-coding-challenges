export const AllCalculations = ({ a, b }) => {
  return (
    <>
      <div> {a + b} </div>
      <div> {a - b} </div>
      <div> {a / b} </div>
      <div> {a * b} </div>
      <div> {a ^ b} </div>
    </>
  );
};
